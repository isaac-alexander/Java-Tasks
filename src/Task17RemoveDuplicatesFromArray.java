// Java Array Program to Remove Duplicate Elements From an Array
// Input: [ 1, 2, 2, 3, 3, 3, 4, 5 ]
// Output: [ 1, 2, 3, 4, 5 ]


public class Task17RemoveDuplicatesFromArray {

    // This method will remove duplicate numbers from the array
    public static int[] removeDuplicates(int[] arr) {

        // Create a new array to store unique numbers (same size as original)
        // Create an index variable to track where to insert unique numbers
        // Loop through the original array one element at a time
        // Inside the loop, check if the current number already exists in the unique array
        // Use another loop to compare current number with numbers already added
        // If the number is already found, skip it
        // If the number is NOT found, add it into the unique array and increase index
        // After the loop, create a final array with the exact number of unique elements
        // Copy only the unique elements into the final result array
        // Return the final array that contains no duplicates

        return new int[0]; // dummy return so the code compiles
    }


    // This method prints the array manually element by element
    public static void printArray(int[] arr) {

        // Print an opening bracket "[ "
        // Loop through the array
        // Print each element followed by a space
        // After the loop, print the closing bracket "]"

    }
}

