// Write a Java program to create an enum called "DaysOfWeek" representing the days of the week.

public class Task26EnumDaysOfTheWeek{
    // This enum contains constants for each day of the week
    enum DaysOfWeek {
        // MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY
    }

    public static void main(String[] args) {
        // Assign a day from the enum to a variable
        // Print the selected day
    }
}
