// Write a Java program that takes a number from the user and generates an integer between 1 and 7. It displays the weekday name.
// Input number: 3
// Expected Output : Wednesday

public class Task23WeekdayName {

    // This method takes an integer dayNumber
    // It returns the corresponding weekday name as a String
    public static String getWeekday(int dayNumber) {
        // Use switch-case to match dayNumber from 1 to 7
        // 1 -> "Monday", 2 -> "Tuesday", ..., 7 -> "Sunday"
        // If dayNumber is not between 1-7, return "Invalid day number"
        return ""; // dummy return
    }

    public static void main(String[] args) {
        // Assign an integer number

        // Call getWeekday with the number
        // Store returned value in a variable

        // Print the weekday
    }
}
